---
import Layout from '../_layouts/DocumentationLayout.astro';
import ContainerBlock from '~/components/blocks/layout/Container.astro';

var metadata = {
  title: {
    default: 'Container',
  },
};
---

<Layout
  metadata={metadata}
  headline={{
    tagline: 'Composite',
    title: 'Container',
    subtitle: 'A flexible container component with layout and background options.',
  }}
>
  <p>
    The ContainerBlock block provides flexible layout control with support for various width
    configurations and rich background options.
  </p>

  <!-- Section 1: ContainerBlock & Content Width -->
  <h2>ContainerBlock & Content Width</h2>
  <p>
    Control how your container and content are sized using <code>containerWidthType</code> and the <code
      >content</code
    > object.
  </p>
  <ul class="list-disc list-inside">
    <li><b>containerWidthType</b>: Controls the outer container (background/section) width.</li>
    <li>
      <b>content.widthType</b>: Controls the inner content width (only when containerWidthType is <code
        >full</code
      >).
    </li>
    <li>
      <b>content.boxWidth</b>: Custom max-width for boxed content (default: <code
        >max-w-[var(--container-max-width)]</code
      >).
    </li>
  </ul>

  <h3>1. Full-bleed background, boxed content (default max-width)</h3>
  <ContainerBlock
    containerWidthType="full"
    content={{
      widthType: 'boxed',
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, boxed content (default max-width).</p>
  </ContainerBlock>

  <h3>2. Full-bleed background, boxed content (custom 50% width)</h3>
  <ContainerBlock
    containerWidthType="full"
    content={{
      widthType: 'boxed',
      boxWidth: 'max-w-1/2',
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, boxed content at <b>50%</b> width.</p>
  </ContainerBlock>

  <h3>3. Full-bleed background, full-width content</h3>
  <ContainerBlock
    containerWidthType="full"
    content={{
      widthType: 'full',
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, full-width content.</p>
  </ContainerBlock>

  <h3>4. Boxed container (background and content boxed)</h3>
  <ContainerBlock
    containerWidthType="boxed"
    content={{ border: { width: 'border', color: 'border-border-emphasis border-dotted' } }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Boxed container (background and content boxed).</p>
  </ContainerBlock>

  <h3>5. Custom container width (background and content custom)</h3>
  <ContainerBlock
    containerWidthType="custom"
    width="w-3/4"
    content={{ border: { width: 'border', color: 'border-border-emphasis border-dotted' } }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">
      Custom container width (background and content custom, e.g., 75%).
    </p>
  </ContainerBlock>

  <!-- Section 2: Backgrounds -->
  <h2>Backgrounds</h2>
  <p>
    Add visual richness with background images, videos, colors, or gradients using the <code
      >background</code
    > prop object.
  </p>

  <h3>1. Background Image (with parallax effect)</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-64"
    background={{
      image: 'https://images.pexels.com/photos/370799/pexels-photo-370799.jpeg',
      attachment: 'fixed',
    }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-12' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 rounded bg-surface/80">
      Full-width container with a <b>fixed</b> background image (parallax effect).
    </p>
  </ContainerBlock>

  <h3>2. Background Video</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ video: 'https://www.pexels.com/download/video/2646392/' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-12' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 rounded bg-surface/80">
      ContainerBlock with a looping <b>background video</b>.
    </p>
  </ContainerBlock>

  <h3>3. Background Color</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ class: 'bg-mauve-600' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-12' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 rounded bg-surface/80">
      ContainerBlock with a solid <b>background color</b> (Tailwind: bg-blue-600).
    </p>
  </ContainerBlock>

  <h3>4. Background Gradient</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ class: 'bg-gradient-to-r from-peach via-pink to-red' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-12' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 rounded bg-surface/80">
      ContainerBlock with a <b>gradient background</b> (Tailwind gradient utilities).
    </p>
  </ContainerBlock>

  <!-- Section 3: Layout -->
  <h2>Layout</h2>
  <p>
    Control how child elements are arranged using the <code>layout</code> prop with Flexbox or Grid.
  </p>

  <h3>1. Flex Row (Horizontal)</h3>
  <ContainerBlock
    containerWidthType="full"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-center justify-between' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded">Item 1</div>
    <div class="bg-green text-white p-4 rounded">Item 2</div>
    <div class="bg-mauve text-white p-4 rounded">Item 3</div>
  </ContainerBlock>

  <h3>2. Flex Column (Vertical)</h3>
  <ContainerBlock
    containerWidthType="full"
    layout={{ type: 'flex', class: 'flex-col gap-4 items-stretch' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded">Item 1</div>
    <div class="bg-green text-white p-4 rounded">Item 2</div>
    <div class="bg-mauve text-white p-4 rounded">Item 3</div>
  </ContainerBlock>

  <h3>3. Grid - 2 Columns</h3>
  <ContainerBlock
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-2 gap-6' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-6 rounded">Column 1</div>
    <div class="bg-green text-white p-6 rounded">Column 2</div>
    <div class="bg-mauve text-white p-6 rounded">Column 3</div>
    <div class="bg-peach text-white p-6 rounded">Column 4</div>
  </ContainerBlock>

  <h3>4. Grid - 3 Columns</h3>
  <ContainerBlock
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-3 gap-4' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded text-center">1</div>
    <div class="bg-green text-white p-4 rounded text-center">2</div>
    <div class="bg-mauve text-white p-4 rounded text-center">3</div>
    <div class="bg-peach text-white p-4 rounded text-center">4</div>
    <div class="bg-pink text-white p-4 rounded text-center">5</div>
    <div class="bg-red text-white p-4 rounded text-center">6</div>
  </ContainerBlock>

  <h3>5. Responsive Grid (2 cols mobile, 4 cols desktop)</h3>
  <ContainerBlock
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-2 md:grid-cols-4 gap-4' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded text-center">1</div>
    <div class="bg-green text-white p-4 rounded text-center">2</div>
    <div class="bg-mauve text-white p-4 rounded text-center">3</div>
    <div class="bg-peach text-white p-4 rounded text-center">4</div>
    <div class="bg-pink text-white p-4 rounded text-center">5</div>
    <div class="bg-red text-white p-4 rounded text-center">6</div>
    <div class="bg-cyan text-white p-4 rounded text-center">7</div>
    <div class="bg-yellow text-white p-4 rounded text-center">8</div>
  </ContainerBlock>

  <h3>6. Min Height with Default Alignment (Top)</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-start' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-mauve text-white p-4 rounded text-center">Item 3</div>
  </ContainerBlock>

  <h3>7. Min Height with Vertical Center</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-center' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-mauve text-white p-4 rounded text-center">Item 3</div>
  </ContainerBlock>

  <h3>8. Min Height with Full Center (Both Axes)</h3>
  <ContainerBlock
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-col gap-4 justify-center items-center' }}
    content={{
      widthType: 'boxed',
      spacing: { padding: 'py-8' },
      border: { width: 'border', color: 'border-border-emphasis border-dotted' },
    }}
    border={{ width: 'border', color: 'border-border' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-mauve text-white p-4 rounded text-center">Item 3</div>
  </ContainerBlock>
</Layout>
