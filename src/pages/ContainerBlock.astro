---
import Container from '~/components/blocks/Container.astro';
import Layout from '../layouts/Layout.astro';

var metadata = {
  title: {
    default: 'Home',
  },
  description: 'Welcome to Astro Blocks - Modern components for the web',
};
---

<Layout metadata={metadata}>
  <h1 class="text-2xl font-bold mb-6">Container Block Documentation & Examples</h1>
  <p class="mb-8">
    The Container block provides flexible layout control with support for various width
    configurations and rich background options.
  </p>

  <!-- Section 1: Container & Content Width -->
  <h2 class="text-3xl font-bold mt-12 mb-4">Container & Content Width</h2>
  <p class="mb-4">
    Control how your container and content are sized using <code>containerWidthType</code> and the <code
      >content</code
    > object.
  </p>
  <ul class="mb-8 list-disc list-inside">
    <li><b>containerWidthType</b>: Controls the outer container (background/section) width.</li>
    <li>
      <b>content.widthType</b>: Controls the inner content width (only when containerWidthType is <code
        >full</code
      >).
    </li>
    <li>
      <b>content.boxWidth</b>: Custom max-width for boxed content (default: <code
        >max-w-[var(--container-max-width)]</code
      >).
    </li>
  </ul>

  <h3 class="text-xl font-semibold mt-8 mb-2">
    1. Full-bleed background, boxed content (default max-width)
  </h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    content={{ widthType: 'boxed', border: { width: 'border', color: 'border-red-500' } }}
    border={{ width: 'border', color: 'border-blue-500' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, boxed content (default max-width).</p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">
    2. Full-bleed background, boxed content (custom 50% width)
  </h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    content={{
      widthType: 'boxed',
      boxWidth: 'max-w-1/2',
      border: { width: 'border', color: 'border-red-500' },
    }}
    border={{ width: 'border', color: 'border-blue-500' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, boxed content at <b>50%</b> width.</p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">3. Full-bleed background, full-width content</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    content={{ widthType: 'full', border: { width: 'border', color: 'border-red-500' } }}
    border={{ width: 'border', color: 'border-blue-500' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Full-width container, full-width content.</p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">4. Boxed container (background and content boxed)</h3>
  <Container
    htmlTag="section"
    containerWidthType="boxed"
    content={{ border: { width: 'border', color: 'border-red-500' } }}
    border={{ width: 'border', color: 'border-blue-500' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">Boxed container (background and content boxed).</p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">
    5. Custom container width (background and content custom)
  </h3>
  <Container
    htmlTag="section"
    containerWidthType="custom"
    width="w-3/4"
    content={{ border: { width: 'border', color: 'border-red-500' } }}
    border={{ width: 'border', color: 'border-blue-500' }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8">
      Custom container width (background and content custom, e.g., 75%).
    </p>
  </Container>

  <!-- Section 2: Backgrounds -->
  <h2 class="text-3xl font-bold mt-12 mb-4">Backgrounds</h2>
  <p class="mb-8">
    Add visual richness with background images, videos, colors, or gradients using the <code
      >background</code
    > prop object.
  </p>

  <h3 class="text-xl font-semibold mt-8 mb-2">1. Background Image (with parallax effect)</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-64"
    background={{
      image: 'https://images.pexels.com/photos/1629236/pexels-photo-1629236.jpeg',
      attachment: 'fixed',
    }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-12' } }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 text-white drop-shadow-lg bg-black/30 rounded">
      Full-width container with a <b>fixed</b> background image (parallax effect).
    </p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">2. Background Video</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ video: 'https://www.pexels.com/download/video/1918465/' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-12' } }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 text-white drop-shadow-lg bg-black/30 rounded">
      Container with a looping <b>background video</b>.
    </p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">3. Background Color</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ class: 'bg-blue-600' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-12' } }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 text-white drop-shadow-lg bg-black/30 rounded">
      Container with a solid <b>background color</b> (Tailwind: bg-blue-600).
    </p>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">4. Background Gradient</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-64"
    background={{ class: 'bg-gradient-to-r from-purple-500 via-pink-500 to-red-500' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-12' } }}
    spacing={{ margin: 'mb-8' }}
  >
    <p class="text-center py-8 text-white drop-shadow-lg bg-black/30 rounded">
      Container with a <b>gradient background</b> (Tailwind gradient utilities).
    </p>
  </Container>

  <!-- Section 3: Layout -->
  <h2 class="text-3xl font-bold mt-12 mb-4">Layout</h2>
  <p class="mb-8">
    Control how child elements are arranged using the <code>layout</code> prop with Flexbox or Grid.
  </p>

  <h3 class="text-xl font-semibold mt-8 mb-2">1. Flex Row (Horizontal)</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-center justify-between' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded">Item 1</div>
    <div class="bg-green-500 text-white p-4 rounded">Item 2</div>
    <div class="bg-purple-500 text-white p-4 rounded">Item 3</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">2. Flex Column (Vertical)</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    layout={{ type: 'flex', class: 'flex-col gap-4 items-stretch' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded">Item 1</div>
    <div class="bg-green-500 text-white p-4 rounded">Item 2</div>
    <div class="bg-purple-500 text-white p-4 rounded">Item 3</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">3. Grid - 2 Columns</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-2 gap-6' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-6 rounded">Column 1</div>
    <div class="bg-green-500 text-white p-6 rounded">Column 2</div>
    <div class="bg-purple-500 text-white p-6 rounded">Column 3</div>
    <div class="bg-orange-500 text-white p-6 rounded">Column 4</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">4. Grid - 3 Columns</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-3 gap-4' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded text-center">1</div>
    <div class="bg-green-500 text-white p-4 rounded text-center">2</div>
    <div class="bg-purple-500 text-white p-4 rounded text-center">3</div>
    <div class="bg-orange-500 text-white p-4 rounded text-center">4</div>
    <div class="bg-pink-500 text-white p-4 rounded text-center">5</div>
    <div class="bg-red-500 text-white p-4 rounded text-center">6</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">
    5. Responsive Grid (2 cols mobile, 4 cols desktop)
  </h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    layout={{ type: 'grid', class: 'grid-cols-2 md:grid-cols-4 gap-4' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded text-center">1</div>
    <div class="bg-green-500 text-white p-4 rounded text-center">2</div>
    <div class="bg-purple-500 text-white p-4 rounded text-center">3</div>
    <div class="bg-orange-500 text-white p-4 rounded text-center">4</div>
    <div class="bg-pink-500 text-white p-4 rounded text-center">5</div>
    <div class="bg-red-500 text-white p-4 rounded text-center">6</div>
    <div class="bg-indigo-500 text-white p-4 rounded text-center">7</div>
    <div class="bg-yellow-500 text-white p-4 rounded text-center">8</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">6. Min Height with Default Alignment (Top)</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-start' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green-500 text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-purple-500 text-white p-4 rounded text-center">Item 3</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">7. Min Height with Vertical Center</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-row gap-4 items-center' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green-500 text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-purple-500 text-white p-4 rounded text-center">Item 3</div>
  </Container>

  <h3 class="text-xl font-semibold mt-8 mb-2">8. Min Height with Full Center (Both Axes)</h3>
  <Container
    htmlTag="section"
    containerWidthType="full"
    minHeight="min-h-96"
    layout={{ type: 'flex', class: 'flex-col gap-4 justify-center items-center' }}
    content={{ widthType: 'boxed', spacing: { padding: 'py-8' } }}
    border={{ width: 'border', color: 'border-gray-300' }}
    spacing={{ margin: 'mb-8' }}
  >
    <div class="bg-blue-500 text-white p-4 rounded text-center">Item 1</div>
    <div class="bg-green-500 text-white p-4 rounded text-center">Item 2</div>
    <div class="bg-purple-500 text-white p-4 rounded text-center">Item 3</div>
  </Container>
</Layout>
